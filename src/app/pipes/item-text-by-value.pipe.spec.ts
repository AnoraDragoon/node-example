import { ItemTextByValuePipe } from './item-text-by-value.pipe';

describe('ItemTextByValuePipe', () => {
  it('create an instance', () => {
    const pipe = new ItemTextByValuePipe();
    expect(pipe).toBeTruthy();
  });
});
