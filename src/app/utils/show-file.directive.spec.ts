import { ShowFileDirective } from './show-file.directive';

describe('ShowFileDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowFileDirective();
    expect(directive).toBeTruthy();
  });
});
